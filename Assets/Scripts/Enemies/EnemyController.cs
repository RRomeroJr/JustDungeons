using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{   
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*for(i = 0; i < SpellList.Count(); i++){
            if(SpellList[i].CD is ready?){
                
                // Resolve SpellList[i] some how
                // maybe SpellList has a some field that corresponds
                // to how the spell should be cast.
                // 0 = onClostest, 1 = onHightestThreat, ect.
                
            }
        }
        */
    }
    // RR: Now that I'm writing this out it would be easier if 
    //     spells had cooldowns before I start doing this

    //void onClosest(Spell)
    //void onHighestThreat(Spell)
    //void onLowestTreat()
    //void atPercentHealth(Spell)
    //void onHealer()
    //void onRanged()
    //void onMelee()
    //void onTank()
}
